[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests that require real models and GPU (skipped by default)
addopts = -v --strict-markers
